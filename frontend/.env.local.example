# ブラウザから画像を直接取得するためのURL(公開情報。静的ファイル配信は認証対象外)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Next.jsサーバー(BFF)からFastAPIへの接続先。docker-compose利用時は http://backend:8000 になる
BACKEND_API_URL=http://localhost:8000

# backend/.env の APP_API_TOKEN と同じ値を設定する(サーバー側のみで使用、ブラウザには渡さない)
APP_API_TOKEN=
